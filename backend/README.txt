# BidWhist Backend API (Local SQLite, Plain JS)

## 1. Install dependencies
cd backend
npm install

## 2. Seed the database (adds tournaments and teams)
npm run seed

## 3. Start the API server
npm start

# Your API runs at http://localhost:5001

# Expose to the web with ngrok (optional)
ngrok http 5001
